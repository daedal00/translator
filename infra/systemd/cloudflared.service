[Unit]
Description=Cloudflare Tunnel
After=network.target

[Service]
User=samuel
ExecStart=/usr/bin/cloudflared tunnel --config /home/samuel/.cloudflared/config.yml run
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
